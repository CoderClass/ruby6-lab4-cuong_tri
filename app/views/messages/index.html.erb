<h1><%= @room.name %></h1>

<ul>  
  <% @messages.each do |message| %>
    <li>  
      <%= message.content %>
    </li>
  <% end %>
</ul>

<%= form_for [@room, Message.new] do |f| %>
  <%= f.label :content %>
  <%= f.text_field :content %>

  <%= f.submit %>
<% end %>
