<h1><%= @room.name %></h1>

<ul class="messages">  
  <%= render @messages %>
</ul>

<%= form_for [@room, Message.new] do |f| %>
  <%= f.label :content %>
  <%= f.text_field :content %>

  <%= f.submit %>
<% end %>

<script>
  setInterval(updateMessagesViaJSON, 3000);
</script>
